# Default server URL baked into native builds. Left empty so a fresh
# install forces users through the server-setup screen — the APK is a
# public artefact and must never ship pre-pointed at anyone's personal
# instance. Override locally (via .env.capacitor.local) if you want
# your own dev builds to start with a default.
VITE_API_URL=
